# Архитектура реализации

## 1. Общая схема

```
Клиент
  ⬓ Нажать кнопку Удалить Неключевые
  ⬓ Вызвать серверную процедуру
  ⬓ Отобразить таблицу результатов
  ⬓ Обработать клики по ссылкам

Sервер
  ⬓ Получить константу ПлатформыРезерв
  ⬓ Натнти тип данных
  ⬓ Преобразовать в массив ссылок
  ⬓ Выполнить SQL-запросы
  ⬓ Удалить неключевые ссылки
  ⬓ Сохранить в константе
  ⬓ Вернуть результаты клиенту
```

## 2. Модуль формы

### 2.1 Подставка ОС и глобальные переменные

```1c
&НаКлиентЕСервере
&Насервере

Переменная ТаблицаПлатформ КАК ТаблицаЗначений;
Переменная УдаленныеПлатформы КАК ТаблицаЗначений;
```

### 2.2 Обработчик кнопки (клиент)

```1c
&НаКлиенте
Процедура УдалитьНеключевые_Клик
    // 1. Вызвать серверную процедуру
    Хранов = Новый Структура;
    ОтделючиедНаНа Сервере = Новые(Знаения);
    НевернутаяВыдалитьПлатформыНаНаНаНа();
    
    // 2. Отобразить результаты
    ОбновитьДанныеФормы();
KонецПроцедуры
```

## 3. Серверные процедуры

### 3.1 Основная процедура очистки

```1c
&Насервере
Процедура ОтделючиедНаНаНаНаНаНа()
    // Основная логика на сервере
    ОтделючиедПлатформы = ПривестиКМассиву(Константы.ПлатформыРезерв.Получить());
KонецПроцедуры
```
